meta {
  name: todo
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/printer/todo
  body: json
  auth: none
}

body:json {
  {
    "items": [
      "Buy groceries",
      "A very long todo item that should wrap to multiple lines because it exceeds the available width",
      "Call the dentist"
    ]
  }
}
